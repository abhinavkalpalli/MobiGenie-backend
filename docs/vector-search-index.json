{
  "_comment": "Pinecone serverless index spec. Created automatically at ai-service boot by PineconeProvider (apps/ai-service/src/ai/pinecone/pinecone.provider.ts) if it doesn't already exist — this file is for reference only.",
  "name": "phone-embeddings",
  "dimension": 384,
  "metric": "cosine",
  "spec": {
    "serverless": {
      "cloud": "aws",
      "region": "us-east-1"
    }
  }
}
